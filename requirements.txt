# VEE Scanner - Booklet Boundary Detection Module
# Dependencies for YOLOv11-based detection pipeline

# Core ML / Detection
ultralytics>=8.3.0          # YOLO11 support (note: package is 'ultralytics', models are 'yolo11*')
torch>=2.0.0
torchvision>=0.15.0

# Computer Vision
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Optimized Inference (CPU fallback)
onnxruntime>=1.16.0

# Testing
pytest>=7.4.0

# PDF Compilation (FR-4.2)
img2pdf>=0.5.0
pikepdf>=8.0.0

# Utilities
tqdm>=4.66.0
